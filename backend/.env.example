PORT=5000
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# PostgreSQL Configuration (Use either DATABASE_URL or individual settings)
# Note: Remove &channel_binding=require if present - it's not supported by pg library
DATABASE_URL=postgresql://neondb_owner:npg_4DSTJuzG8NbK@ep-autumn-queen-adrfn54i-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require

# Alternative: Individual settings (not needed if using DATABASE_URL)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=dating_app
# DB_USER=postgres
# DB_PASSWORD=your-password-here

# Frontend URL used to build password reset links (optional)
FRONTEND_URL=http://localhost:3000

# SMTP settings (production)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey_or_username
# SMTP_PASS=your_smtp_password
# SMTP_FROM=Pairfect <no-reply@yourdomain.com>

# If SMTP_* are not set, the server will use an Ethereal test account in development and log a preview URL.
